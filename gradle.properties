# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G

# Gradle 9 requires JVM 17+ to run its daemon. The build uses Java 21+ toolchains,
# so point the Gradle daemon at a recent JDK that can launch it.
# Adjust this path if your JDK is installed elsewhere.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-21.0.12.101-hotspot